# Plans

This directory stores project plans by lifecycle state.

Create new plans under `active/`. When a plan finishes successfully, move it to `completed/`. When a plan fails or is closed without successful completion, move it to `failed/`.

Keep each plan in the directory that matches its current lifecycle state.
