# Failed Plans

This directory stores plans whose work has failed or has been stopped without successful completion.

Move a plan into this directory only after the plan's lifecycle rules confirm that the work should be closed as failed.
